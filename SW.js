// Este é um Service Worker básico apenas para satisfazer os requisitos de instalação de um PWA.
self.addEventListener('fetch', function(event) {
    // Não faz nada complexo, apenas deixa as requisições passarem normalmente
});